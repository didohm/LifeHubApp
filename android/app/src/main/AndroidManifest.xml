<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Restore notifications after device restart -->
        <receiver
            android:name="com.capacitorjs.plugins.localnotifications.LocalNotificationRestoreReceiver"
            android:exported="true"
            android:directBootAware="true"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Doze-exempt ticker for Walk Service.
             Receives AlarmManager broadcasts to update metrics (distance, steps, duration)
             every second even when device is in deep Doze mode. This keeps the walk
             notification and JS app metrics fresh during background tracking. -->
        <receiver
            android:name=".WalkTickerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.lifehub.app.walk.TICKER" />
            </intent-filter>
        </receiver>

        <!-- Foreground Service for Walk Tracking.
             stopWithTask="false" keeps the walk alive (and the notification
             persistent) when the app is swiped away — the service re-enters the
             foreground on its own after a process death, recovering the walk
             from persisted state instead of silently dropping it. -->
        <service
            android:name=".WalkService"
            android:foregroundServiceType="location|health"
            android:exported="false"
            android:stopWithTask="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network state (ConnectivityMonitor — walk provider switching) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location (Walk Service - GPS tracking) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Notifications (Reminders for appointments, medications, bills, etc.) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Storage (Document uploads) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Camera (optional - for document scanning, requested at runtime only) -->
    <uses-permission android:name="android.permission.CAMERA" android:required="false" />

    <!-- Audio (voice / audio features) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Wake lock (keeps notifications working when app is backgrounded) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Step counter & Activity Recognition (Walk service step counter) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- Foreground services for background tracking -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />

    <!-- Battery optimization exemption (Doze / OEM battery managers would
         otherwise suspend the walk service's location + step streams minutes
         into a walk, freezing steps/distance while the screen is off). The
         app requests the exemption at walk start. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Declare the step-counter hardware so the OS/manufacturers know the
         app uses it (optional — the service degrades to GPS-only otherwise). -->
    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.stepdetector"
        android:required="false" />

</manifest>
